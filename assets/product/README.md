# Master product photo

`master-product.png` — the official product photograph (pistachio sweets), cropped to
remove the source camera watermark and border artifacts. Composited into the middle
band of every generated label by `src/backend/src/lib/composite.ts`.

Replace this file (same name) and regenerate to update every label at once.
